fn main(){
  // Constants represents values that cannot be changed. If you declare
  // a constant then there is no way its value changes.
  const PI:f32=3.14; // declare float constant
  const NUM: i32=100; // declare integer constant
  println!("{},{}",PI,NUM);
}
